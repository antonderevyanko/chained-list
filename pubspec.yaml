name: chained_list
description: "Library for Flutter List tiles with visual connection. Provides convenient way of lists with leading 'metro-lines' widget."
version: 0.8.2
homepage: https://github.com/antonderevyanko/chained-list

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
